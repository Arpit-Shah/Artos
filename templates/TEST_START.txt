${:import(com.arpit.framework.Start, com.arpit.infra.TestContext, com.arpit.interfaces.TestExecutor)}

public class START implements TestExecutor {

	public void onExecute(TestContext context) throws Exception {
		Start s = new Start();
		s.onExecute(context, START.class, "${user}", "${date}", "");
	}
}